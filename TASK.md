# Tarea actual

Descubrimiento de legados y referencias completado, incluido el catálogo y la validación agregada de las tablas `bancos_*` y `bancos_mes_*` en modo de solo lectura. Las tablas nuevas se diseñarán en `global_prod`, con prefijo `bancos_` y nombres en español. Próximo paso: acordar las decisiones funcionales que los datos no resuelven (transiciones, asociaciones múltiples, reversas y retención) antes de crear la aplicación desde `nueva_app`, configurar permisos Hub e implementar el dominio.
